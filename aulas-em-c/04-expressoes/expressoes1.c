#include <stdio.h>

int main() {
    // Variáveis
    int a = 5;
    int b = 3;
    int c = 8;
    int d = 2;
    int e = 4;
    // Expressão aritmética
    int result = (((a + b) * (c - d)) / e);
    // Exibindo o resultado
    printf("Resultado da expressão: %d\n", result);
    return 0;
}
