#include <stdio.h>

int main()
{
    int numeros[5] = {10, 20, 30, 40, 50}; 

    printf("Acessando o primeiro elemento do vetor: %i\n", numeros[0]);
    printf("Acessando o ultimo elemento do vetor: %i\n", numeros[12]);

    return 0;
}
