#include <stdio.h>

int main() {
    int vetor[5] = {10, 20, 30, 40, 50};  // Declaração e inicialização do vetor
    
    // Acessando elementos do vetor
    printf("Primeiro elemento: %d\n", vetor[0]); // Acessa o índice 0
    printf("Quarto elemento: %d\n", vetor[3]);   // Acessa o índice 3
    
    return 0;
}