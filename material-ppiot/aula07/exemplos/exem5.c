#include <stdio.h>

int main() {
    int contador = 1;

    // Exemplo de laço do-while
    do {
        printf("Contador: %d\n", contador);
        contador++;
    } while (contador <= 5);

    return 0;
}